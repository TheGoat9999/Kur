import type {Database} from '../db.js';
import {NeedsMutationResultSchema,NeedsStateSchema,type NeedsState,type RestKind} from '@sol-dorado/contracts/needs';
import {applyNeedsTicks,NEEDS_TICK_MINUTES,shouldBeUnconscious} from '../domain/needs.js';
export class NeedsCommandError extends Error{constructor(public readonly code:string,public readonly status:number){super(code);}}
async function ensureRuntime(db:Database,playerId:string){await db.query(`INSERT INTO player_needs_runtime(player_id) VALUES($1) ON CONFLICT(player_id) DO NOTHING`,[playerId]);}
export async function simulateNeeds(db:Database,playerId:string){const client=await db.connect();try{await client.query('BEGIN');await client.query(`INSERT INTO player_needs_runtime(player_id) VALUES($1) ON CONFLICT(player_id) DO NOTHING`,[playerId]);await client.query(`DELETE FROM player_injuries WHERE player_id=$1 AND treated=true AND recovery_until IS NOT NULL AND recovery_until<=now()`,[playerId]);const runtime=(await client.query(`SELECT * FROM player_needs_runtime WHERE player_id=$1 FOR UPDATE`,[playerId])).rows[0];const state=(await client.query(`SELECT health,energy,satiety,hydration,stress FROM player_state WHERE player_id=$1 FOR UPDATE`,[playerId])).rows[0];if(!state)throw new NeedsCommandError('player_not_found',404);const bleed=Number((await client.query(`SELECT COALESCE(MAX(bleeding),0) bleeding FROM player_injuries WHERE player_id=$1`,[playerId])).rows[0]?.bleeding??0);const ticks=Math.floor((Date.now()-new Date(runtime.last_simulated_at).getTime())/(NEEDS_TICK_MINUTES*60000));if(ticks>0){const next=applyNeedsTicks(state,ticks,bleed);await client.query(`UPDATE player_state SET health=$2,energy=$3,satiety=$4,hydration=$5,stress=$6,version=version+1,updated_at=now() WHERE player_id=$1`,[playerId,next.health,next.energy,next.satiety,next.hydration,next.stress]);await client.query(`UPDATE player_needs_runtime SET consciousness=CASE WHEN $2 THEN 'unconscious' ELSE consciousness END,last_simulated_at=last_simulated_at+($3*interval '15 minutes'),updated_at=now() WHERE player_id=$1`,[playerId,shouldBeUnconscious(next,bleed),ticks]);}await client.query(`UPDATE player_needs_runtime SET care_state='field',admitted_until=NULL,consciousness='conscious',updated_at=now() WHERE player_id=$1 AND care_state='admitted' AND admitted_until<=now()`,[playerId]);await client.query('COMMIT');}catch(e){await client.query('ROLLBACK');throw e;}finally{client.release();}}
export async function getNeedsState(db:Database,playerId:string):Promise<NeedsState>{await simulateNeeds(db,playerId);await ensureRuntime(db,playerId);const [r,s,i,h]=await Promise.all([db.query(`SELECT consciousness,care_state,pain,admitted_until,last_simulated_at FROM player_needs_runtime WHERE player_id=$1`,[playerId]),db.query(`SELECT health,energy,satiety,hydration,stress FROM player_state WHERE player_id=$1`,[playerId]),db.query(`SELECT id,kind,body_area,severity,bleeding,treated,recovery_until,created_at FROM player_injuries WHERE player_id=$1 ORDER BY severity DESC,created_at DESC`,[playerId]),db.query(`SELECT 1 FROM player_properties pp JOIN real_estate_properties p ON p.id=pp.property_id WHERE pp.player_id=$1 AND pp.is_primary_residence=true AND p.kind IN('apartment','house') LIMIT 1`,[playerId])]);const runtime=r.rows[0],hud=s.rows[0];if(!runtime||!hud)throw new NeedsCommandError('player_not_found',404);const injuries=i.rows.map((x:any)=>({id:x.id,kind:x.kind,bodyArea:x.body_area,severity:x.severity,bleeding:x.bleeding,treated:x.treated,recoveryUntil:x.recovery_until?new Date(x.recovery_until).toISOString():null,createdAt:new Date(x.created_at).toISOString()}));const bleeding=injuries.reduce((m:number,x:any)=>Math.max(m,x.bleeding),0);const canAct=runtime.consciousness==='conscious'&&runtime.care_state==='field';return NeedsStateSchema.parse({status:{consciousness:runtime.consciousness,careState:runtime.care_state,pain:runtime.pain,exhausted:hud.energy<=15,hungry:hud.satiety<=20,dehydrated:hud.hydration<=20,bleeding,canRest:canAct,canSleep:canAct&&h.rowCount===1,primaryResidenceRequired:h.rowCount!==1,admittedUntil:runtime.admitted_until?new Date(runtime.admitted_until).toISOString():null,injuries,lastSimulatedAt:new Date(runtime.last_simulated_at).toISOString()},hud});}
export async function restPlayer(db:Database,playerId:string,kind:RestKind){await simulateNeeds(db,playerId);const client=await db.connect();try{await client.query('BEGIN');const r=(await client.query(`SELECT consciousness,care_state FROM player_needs_runtime WHERE player_id=$1 FOR UPDATE`,[playerId])).rows[0];if(r.consciousness!=='conscious')throw new NeedsCommandError('unconscious_cannot_rest',409);if(r.care_state!=='field')throw new NeedsCommandError('care_state_blocks_rest',409);if(kind==='sleep'){const home=await client.query(`SELECT 1 FROM player_properties pp JOIN real_estate_properties p ON p.id=pp.property_id WHERE pp.player_id=$1 AND pp.is_primary_residence=true AND p.kind IN('apartment','house') LIMIT 1`,[playerId]);if(!home.rowCount)throw new NeedsCommandError('primary_residence_required',409);}const bleed=Number((await client.query(`SELECT COALESCE(MAX(bleeding),0) bleeding FROM player_injuries WHERE player_id=$1`,[playerId])).rows[0]?.bleeding??0);if(kind==='sleep'&&bleed>=2)throw new NeedsCommandError('major_bleeding_requires_treatment',409);await client.query(`UPDATE player_state SET energy=LEAST(100,energy+$2),stress=GREATEST(0,stress-$3),health=LEAST(100,health+$4),satiety=GREATEST(0,satiety-$5),hydration=GREATEST(0,hydration-$6),version=version+1,updated_at=now() WHERE player_id=$1`,[playerId,kind==='sleep'?60:22,kind==='sleep'?22:8,bleed===0?(kind==='sleep'?8:2):0,kind==='sleep'?4:1,kind==='sleep'?6:2]);await client.query(`UPDATE player_needs_runtime SET pain=GREATEST(0,pain-$2),last_rest_at=now(),updated_at=now() WHERE player_id=$1`,[playerId,kind==='sleep'?15:5]);await client.query('COMMIT');}catch(e){await client.query('ROLLBACK');throw e;}finally{client.release();}return NeedsMutationResultSchema.parse({needs:await getNeedsState(db,playerId),noticeBg:kind==='sleep'?'Сънят възстанови енергията ти, но увеличи глада и жаждата.':'Почивката възстанови част от енергията ти.',noticeEn:kind==='sleep'?'Sleep restored energy, but increased hunger and thirst.':'Rest restored some energy.'});}
